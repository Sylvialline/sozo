# Directory tree

递归输出目录树，忽略指定目录，并按扩展名统计文件数量和总字节数。

```powershell
python samples/13_example_tasks/directory_tree/solution.py
```

默认忽略 `.cache`。期望结果见 [`output_expected/result.txt`](output_expected/result.txt)。
