# Simple interpreter

逐行解释整数赋值语句，支持变量、括号、加法、减法和乘法。实现包含 tokenizer、
递归下降 parser 和环境对象，不使用 `eval`。

```powershell
python samples/13_example_tasks/simple_interpreter/solution.py
```

期望结果见 [`output_expected/result.txt`](output_expected/result.txt)。
