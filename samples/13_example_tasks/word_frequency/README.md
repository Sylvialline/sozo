# Word frequency

批量读取 `input/*.txt`，忽略大小写和标点，用 `Counter` 输出最高频单词。

```powershell
python samples/13_example_tasks/word_frequency/solution.py
```

期望结果见 [`output_expected/result.txt`](output_expected/result.txt)。
