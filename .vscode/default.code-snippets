{
	"Python exam default": {
		"scope": "python",
		"prefix": "exam-default",
		"body": [
			"from pathlib import Path",
			"import sys",
			"",
			"HERE, ROOT = Path(__file__).resolve().parents[:2]",
			"sys.path.insert(0, str(ROOT))",
			"DATA = HERE / \"data\"",
			"# OUTPUT = HERE / \"output\"",
			"# OUTPUT.mkdir(exist_ok=True)",
			"",
			"from utils import Case, Exam",
			"$0"
		],
		"description": "Insert the default Python exam file setup"
	}
}
